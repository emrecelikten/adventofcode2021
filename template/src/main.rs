use common::error::CommonError;

fn main() -> Result<(), CommonError> {
    Ok(())
}

#[cfg(test)]
mod tests {
    use super::*;

    const TEST_DATA: &'static str = r"";

    #[test]
    fn test() {

    }

}